/**
 * An entity of Project
 */
export interface ProjectField {
    /**
     * A project name.
     */
    projectName: string;
    /**
     * A path of placed the project.
     * This is similar with `[Memoma projectPath]()`.
     */
    filePath: string;
}
